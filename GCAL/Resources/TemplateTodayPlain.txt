# (template TodayPlain
## comment is with two starting ##
## setting of parsing characters for printed texts
## these parsing constants are used for lines without preceding #
#   (set markupStart '{ )
#   (set markupEnd   '} )
#   (set escapeChar  '\ )
#   (set acom today.dateHumanName) 
#   (if (gt acom.length 0)  
#       (set acom (string "- "  acom))
#   ) 
{today.date.longDateString} {acom}

# )